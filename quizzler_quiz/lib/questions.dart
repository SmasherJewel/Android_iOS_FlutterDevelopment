class Questions {
  String questionText;
  bool questionAnswer;
  // Create a Constructor
  Questions({
    required String q,
    required bool a,
  })  : questionText = q,
        questionAnswer = a;
}
